language: node_js
node_js:
  - 12
install:
  - yarn install
script:
  - yarn config set ignore -engines true
  - yarn docs:build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: docs/.vuepress/dist
  github_token: $blog
  keep_history: true
  on:
    branch: master
